server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;
  location / { try_files $uri $uri/ /index.html; }
  location /graphql { proxy_pass http://graphql:4000; proxy_set_header Host $host; }
  location /api/   { proxy_pass http://graphql:4000; }
}
